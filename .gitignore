node_modules/
.pnpm-home/
.pnpm-store/
.next/
.turbo/
out/
dist/
.vercel/
coverage/
.playwright-mcp/
test-results/
playwright-report/
tsconfig.tsbuildinfo

.env
.env.local
.env*.local
.env.*.local

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Референсы: в репозитории живут только выбранные — они часть спецификации редизайна.
# Промежуточные генерации и отвергнутые варианты остаются локальными.
/docs/references/*
!/docs/references/selected/

# файл окружения с пробелами в имени — иначе шаблон .env его не ловит
.env\ \ 

# Инструментарий для видео. Он лежит в этой папке по случайности, а не по смыслу, и к продукту
# отношения не имеет — см. раздел о двух делах в CLAUDE.md. Игнорируется намеренно: правило,
# записанное словами, однажды уже не сработало, и скрипт уехал в историю сайта заодно с чужим
# проходом. Строка делает границу механической, а не рассчитанной на внимательность.
/scripts/telegram-notify.mjs
