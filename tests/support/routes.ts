/**
 * Маршруты публичного слоя для тестов — в одном месте.
 *
 * Публичный слой разделён надвое: `/` — входной экран, содержание живёт на `/platform`. Тесты,
 * которым нужен лендинг, обязаны ходить именно туда; шестнадцать разбросанных по файлам `"/"`
 * при следующем изменении маршрута снова разъехались бы поодиночке.
 */
export const ENTRY = "/";
export const SITE = "/platform";

/** Якорь раздела на основном сайте. */
export const siteSection = (hash: string) => `${SITE}${hash}`;
