# Как писать тесты в этом проекте

Читается, когда работа идёт с тестами. Уроки перенесены из корневого `CLAUDE.md`.

- **Тест, который не может упасть, хуже отсутствующего.** Проверка сравнивала подпись стиля,
  которую правка не меняла, — была зелёной и пропустила баг с отменой правок. При написании
  ожидания спрашивать: что должно сломаться, чтобы оно покраснело?
- **Тест, который сеет данные через `addInitScript`, не может проверять сохранение.** Скрипт
  выполняется при КАЖДОЙ загрузке, поэтому после F5 он возвращает исходные данные поверх того,
  что записало приложение. Такой тест зелен и с полностью сломанной записью. Для проверок
  сохранения посев должен ставиться только в пустое хранилище.
