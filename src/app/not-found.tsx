import type { Metadata } from "next";
import Link from "next/link";

/**
 * Общий 404 публичного слоя.
 *
 * До этого прохода его не было вовсе, и неизвестный адрес отдавал стандартную страницу Next —
 * чёрный текст на белом, латиницей, без единого признака AEVIX. Человек, ошибшийся в ссылке,
 * видел не «страницы нет», а «сайт сломан».
 *
 * Это НЕ то же самое, что «проект не найден» в Workspace: там речь про конкретную сущность,
 * которой нет или которая принадлежит не вам, и ответ ведёт к списку проектов. Здесь адреса нет
 * во всём приложении, и ответ ведёт наружу — на входной экран и на сайт. Смешивать их нельзя:
 * иначе один случай начнёт объяснять человеку другой.
 *
 * Про приватное здесь не говорится ничего: страница не знает, существует ли где-то проект с
 * таким адресом, и не должна узнавать — «нет доступа» и «нет страницы» для чужого выглядят
 * одинаково намеренно.
 */
export const metadata: Metadata = {
  title: "Страница не найдена",
  // Ошибочный адрес не место в выдаче — даже если на него откуда-то ведёт ссылка.
  robots: { index: false, follow: false },
};

export default function NotFound() {
  return (
    <main className="notfound-page">
      <div className="notfound-inner">
        <p className="notfound-code">404</p>
        <h1 className="notfound-title">Такой страницы нет</h1>
        <p className="notfound-lead">
          Возможно, адрес набран с опечаткой или ссылка устарела. Всё, что есть у AEVIX, доступно
          с двух страниц ниже.
        </p>
        <div className="notfound-actions">
          <Link href="/" className="notfound-action is-primary">
            На главную
          </Link>
          <Link href="/platform" className="notfound-action">
            Возможности и цены
          </Link>
        </div>
      </div>
    </main>
  );
}
